{
  "name": "guance-windows-electron-sample",
  "version": "0.1.0",
  "private": true,
  "productName": "Guance Windows Electron Sample",
  "description": "Electron hybrid UI acceptance sample for the Guance Windows observability SDK",
  "config": {
    "windowsArtifactName": "GuanceWindowsElectronSample"
  },
  "main": "src/main/main.cjs",
  "scripts": {
    "electron:install": "node node_modules/electron/install.js",
    "dev": "npm run electron:install && npm run build && concurrently --kill-others --success command-electron --names renderer,electron \"vite --host 127.0.0.1\" \"wait-on http-get://127.0.0.1:5173 && cross-env ELECTRON_RENDERER_URL=http://127.0.0.1:5173 electron .\"",
    "build": "tsc --noEmit && vite build",
    "native:build": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File ../../build/native-windows-cmake.ps1 -TargetArch x64 -Configuration Release",
    "start": "npm run electron:install && npm run build && electron .",
    "smoke": "npm run electron:install && npm run build && cross-env ELECTRON_SMOKE=1 electron .",
    "package:win": "npm run native:build && node scripts/package-windows.mjs",
    "verify:package:win": "node scripts/verify-windows-package.cjs",
    "verify:package:win:files": "node scripts/verify-windows-package.cjs --skip-runtime",
    "acceptance:win": "npm run electron:install && npm run build && npm run package:win && npm run verify:package:win",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@cloudcare/browser-logs": "^3.3.6",
    "@cloudcare/browser-rum": "^3.3.5"
  },
  "devDependencies": {
    "@electron/packager": "20.0.4",
    "@types/node": "^24.0.0",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "22.3.27",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.0.18",
    "wait-on": "^8.0.3"
  },
  "engines": {
    "node": ">=22.12"
  }
}
